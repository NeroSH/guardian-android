<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Window chrome only; actual UI is themed by Compose MaterialTheme (GuardianTheme). -->
    <style name="Theme.Guardian" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
    <!-- Cold-start system splash (core-splashscreen). Rotating animated-vector icon on a fixed
         brand background, then hands off to Theme.Guardian + the in-app Compose splash. -->
    <style name="Theme.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/avd_anim</item>
        <item name="windowSplashScreenAnimationDuration">1000</item>
        <item name="postSplashScreenTheme">@style/Theme.Guardian</item>
    </style>
</resources>
